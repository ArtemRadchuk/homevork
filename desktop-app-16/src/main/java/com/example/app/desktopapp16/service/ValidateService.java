package com.example.app.desktopapp16.service;

import javafx.scene.control.TextField;

public interface ValidateService {
    boolean validate(TextField surName, TextField name, TextField patronymic);
}
